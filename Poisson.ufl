
element = VectorElement("Lagrange", quadrilateral, 1)

u = TrialFunction(element)
v = TestFunction(element)

f = Coefficient(element)

a = inner(u, v)*dx
L = inner(f, v)*dx 

#    ffc -l dolfin Poisson.ufl
